package com.example.models;

/**
 * Defines the singleplayer game modes.
 * VS_SELF  — 1 human player, survive as long as possible.
 * VS_AI    — 1 human + 3 AI opponents (default).
 */
public enum GameMode {
    VS_SELF,
    VS_AI
}

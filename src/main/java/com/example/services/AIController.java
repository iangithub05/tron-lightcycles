package com.example.services;

/**
 * Marker interface for all AI controllers.
 * GameSession uses this to apply the AI tick cooldown/interval.
 */
public interface AIController extends Controller {
    // No additional methods.
}
